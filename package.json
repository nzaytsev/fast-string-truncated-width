{
  "name": "@nzaytsev/fast-string-truncated-width",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nzaytsev/fast-string-truncated-width.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "description": "A fast function for calculating where a string should be truncated, given an optional width limit and an ellipsis string.",
  "version": "1.1.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "benchmark": "tsex benchmark",
    "benchmark:watch": "tsex benchmark --watch",
    "clean": "tsex clean",
    "compile": "tsex compile",
    "compile:watch": "tsex compile --watch",
    "test": "tsex test",
    "test:watch": "tsex test --watch",
    "prepare": "tsex compile"
  },
  "keywords": [
    "fast",
    "string",
    "truncated",
    "width",
    "cli",
    "terminal"
  ],
  "devDependencies": {
    "benchloop": "^2.1.1",
    "fava": "^0.3.2",
    "tsex": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
